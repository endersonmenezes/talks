# Exemplo mínimo da dinâmica da Carreira 02.
# O build FALHA de propósito (DebugExample.java tem 5 erros) —
# é a demonstração da camada "compilação" da jornada de debug.
#
# Docker:            docker compose -f compose-example.yaml build
# Podman:            podman-compose -f compose-example.yaml build

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.example
