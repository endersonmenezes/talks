# Exemplo mínimo da dinâmica da Carreira 02.
# O build FALHA de propósito (ExemploDebug.java tem 5 erros) —
# é a demonstração da camada "compilação" da jornada de debug.
#
# Docker:            docker compose -f compose-exemplo.yaml build
# Podman:            podman-compose -f compose-exemplo.yaml build

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.exemplo
